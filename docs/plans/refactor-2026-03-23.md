> **SUPERSEDED** — This refactor plan assumed removing seven engines and an `EngineKind`-based core. The workspace instead adopted **ADR-014** (`DepartmentApp` + string IDs; **`EngineKind` removed**), kept the business engines as thinner departments, and uses a dynamic dept route. **Do not execute** the steps below.

# Refactoring Plan — 2026-03-23 (historical)

**Current architecture and metrics:** [`../status/current-state.md`](../status/current-state.md), [`../design/architecture-v2.md`](../design/architecture-v2.md), [`../design/decisions.md`](../design/decisions.md).

The original file body has been retired to avoid conflicting with shipped wiring.
