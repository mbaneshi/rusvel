# Codebase Audit — 2026-03-23

## Summary

Full audit of RUSVEL codebase covering Rust backend (27 crates at time of audit, grew to 34 by 2026-03-24, now 48 as of 2026-03-26). Updated 2026-03-24 with fix statuses, 2026-03-26 with post-audit updates.

---

## Critical Issues

### C1. Dynamic Tailwind Classes Broken in Production

**File:** `frontend/src/lib/components/chat/DepartmentPanel.svelte`
**Lines:** ~30 occurrences using `bg-{color}-600/20`, `text-{color}-400`, `border-{color}-500/30`, etc.

**Problem:** Tailwind CSS purges at build time. Template-literal class names like `bg-${color}-600/20` are never in the source as literal strings, so the CSS is not generated. Colors render correctly in dev (JIT) but break in production builds.

**Fix:** Replace with a static class mapping object keyed by color name.

**Status:** FIXED

---

### C2. SSE Stream Parsing Duplicated 3x in api.ts

**File:** `frontend/src/lib/api.ts`
**Functions:** `streamDeptChat` (L195-234), `streamCapability` (L471-509), `streamHelp` (L513-551), `streamChat` (L555-612)

**Problem:** Four functions contain identical SSE parsing logic (fetch → reader → decode → split lines → parse JSON → dispatch callbacks). Any bug fix or protocol change must be applied in 4 places.

**Fix:** Extract shared `parseSSE()` helper.

**Status:** FIXED

---

### C3. 12 Redundant Static Department Routes

**Files:** `frontend/src/routes/{forge,code,content,harvest,gtm,finance,product,growth,distro,legal,support,infra}/+page.svelte`

**Problem:** All 12 files are nearly identical (~44 lines each, differing only in config data). The dynamic route `frontend/src/routes/dept/[id]/+page.svelte` already exists and the layout navigates to `/dept/{id}`. The static routes are dead code.

**Fix:** Delete all 12 static route directories. The `dept/[id]` route handles everything via the department registry API. Enhance the dynamic route to pass `helpDescription` and `helpPrompts` from the registry.

**Status:** FIXED

---

## High Priority Issues

### H1. CLAUDE.md Severely Outdated

**Status:** FIXED (updated 2026-03-24)

CLAUDE.md now reflects: 48 crates, 222 tests in 30 binaries, 124 API handlers across 23 modules, 19 port traits (14 Port + 5 Store), 82 domain types, 13 engines (5 wired + 8 stubs), ~43,670 lines Rust across 185 source files. (Updated 2026-03-26.)

---

### H2. ForgeEngine Test Mocks Use `unimplemented!()`

**File:** `crates/forge-engine/src/lib.rs` (test module)

**Problem:** `MockStorage` returns `unimplemented!()` for `events()`, `sessions()`, `jobs()`, `metrics()`. If any test path touches these, it panics.

**Fix:** Return minimal stub implementations.

**Status:** FIXED

---

### H3. Eight Engines Not Fully Wired in main.rs

**Engines:** gtm, finance, product, growth, distro, legal, support, infra

**Situation:**
- All 8 have domain code (submodules with managers, ~500-750 lines each)
- ~~`EngineKind` enum has all 12 variants + Flow~~ (removed in ADR-014, string IDs used)
- `DepartmentRegistry` defines all 12 departments
- Frontend routes serve all 12
- API handles all 12 via parameterized routes
- 5 engines ARE wired: Forge, Code, Content, Harvest, Flow
- 8 engines remain stubs

**Impact:** The 8 stub engines exist as crates but their domain logic is never called. Chat works for all departments (routed through the generic agent), but engine-specific domain operations don't execute.

**Decision:** Keep — they represent the target architecture. The generic agent handles chat for all departments regardless of whether a dedicated engine is instantiated. Engine instantiation will be added incrementally as domain features mature.

**Update (2026-03-24):** CodeEngine, ContentEngine, HarvestEngine, and FlowEngine are now fully wired with real domain logic, API routes, CLI commands, and job handlers.

---

### H4. Job Queue Has No Worker Loop

**Status:** FIXED

Background job worker now polls every 5 seconds in main.rs. Handles CodeAnalyze, ContentPublish, HarvestScan via real engines with session_id scoping.

---

### H5. Approval Workflow Partially Complete

**Status:** FIXED (API + UI shipped 2026-03-26)

API endpoints exist (`GET /api/approvals`, approve/reject). Job worker respects approval gates. Frontend ApprovalCard and ApprovalQueue components now shipped.

---

## Medium Priority Issues

### M1. `#[allow(dead_code)]` on All Engine Structs

All 7 domain engines and ForgeEngine suppress dead-code warnings on stored port fields. This is intentional — ports are injected for future use as engine features mature.

### M2. DepartmentPanel.svelte Is Large (~666 lines)

Handles agents, skills, rules, MCP, hooks, workflows, capability engine, directories, and events in a single component. Consider splitting into subcomponents when adding features.

### M3. No Analytics Frontend

`/api/analytics` endpoint returns data (agents, skills, rules, events counts) but no frontend component consumes it. The dashboard page could display this.

---

## Low Priority / Code Quality

### L1. Build Warnings

Run `cargo build 2>&1 | grep warning` to identify current warnings. The earlier analysis found potential unused imports, but verification showed they were false positives — all imports in `chat.rs` and `runway.rs` are used.

### L2. Accessibility Gaps

- Chat messages use `<div>` instead of semantic `<article>` with ARIA roles
- Toggle states (rules/hooks on/off) communicated by color only
- Many form inputs use `placeholder` as label
- No skip navigation links

### L3. Manual Store Subscriptions

Multiple components use `store.subscribe((v) => (local = v))` instead of Svelte's `$store` auto-subscription. This is a style choice but creates more boilerplate. Note: Svelte 5 runes (`$state`, `$derived`) are already used elsewhere, but stores predate this migration.

---

## What Was NOT Wrong (False Positives from Initial Analysis)

| Claim | Reality |
|-------|---------|
| `EmptyState.svelte` broken Icon import | Icon directory exists at `components/icons/`, import is valid |
| Unused imports in `chat.rs` | All imports used |
| Unused imports in `runway.rs` | All imports used |
| XSS via Streamdown markdown | Streamdown uses safe rendering; content comes from our own API |

---

## Post-Audit Updates (2026-03-26)

Significant architectural changes since the original audit:

### Crate count: 34 → 48
14 new crates added: 13 `dept-*` crates (DepartmentApp implementations), `rusvel-engine-tools` (12 engine-specific tools), `rusvel-terminal` (TerminalPort adapter).

### ADR-014: DepartmentApp Pattern
- `EngineKind` enum **removed** entirely — string IDs used everywhere
- New `DepartmentApp` trait in `rusvel-core::department` with `manifest()`, `register()`, `shutdown()`
- New `DepartmentManifest` struct declares routes, tools, capabilities, dependencies
- `RegistrationContext` provides host subsystems to departments during boot
- Each department is a self-contained `dept-*` crate wrapping its engine

### AgentRuntime Replaced ClaudeCliStreamer
- `AgentRuntime::run_streaming()` returns `mpsc::Receiver<AgentEvent>`
- Multi-turn tool loop: LLM → tool calls → execute → feed back → repeat
- `AgentEvent` variants: TextDelta, ToolCall, ToolResult, Done, Error
- `LlmStreamEvent` + `stream()` on `LlmPort` for incremental model output

### 22+ Registered Tools
- 9 built-in tools (rusvel-builtin-tools)
- 12 engine-specific tools (rusvel-engine-tools)
- `tool_search` meta-tool for runtime discovery
- `ScopedToolRegistry` for per-department tool visibility + deferred loading

### Approval UI Shipped
- Frontend `ApprovalCard` and `ApprovalQueue` components
- `ToolCallCard` component for displaying tool invocations in chat
- H5 from this audit is now fully resolved

### Terminal Phase 1
- `TerminalPort` trait added to rusvel-core (port count now 19: 14 Port + 5 Store)
- `rusvel-terminal` adapter crate
- Foundation for agent-driven shell command execution

### Other Changes
- `ModelTier` routing (Quick/Standard/Premium) in rusvel-llm
- `CostTracker` for per-session token/cost tracking
- Manifest-aligned frontend navigation
- 222 tests in 30 binaries, 0 failures (up from 118 at time of audit)
- ~43,670 lines of Rust (was ~34,286)
- 124 API handler functions across 23 modules (was 79 routes)
- 185 source files
