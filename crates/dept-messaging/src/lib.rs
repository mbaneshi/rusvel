//! Placeholder department crate for messaging — compose [`rusvel_channel::ChannelPort`] in `rusvel-app` last.

/// Marker type until `DepartmentApp` wiring for external channels lands.
#[derive(Debug, Default)]
pub struct MessagingDepartmentShell;
