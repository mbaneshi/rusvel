[project]
name = "rusvel-scripts"
version = "0.1.0"
description = "Python scripts and utilities for RUSVEL"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
analysis = [
    "anthropic>=0.52.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.0",
]
