---
paths:
  - "docs/**/*.md"
---

# Documentation Rules

- Use ATX-style headers (# not underlines)
- Code blocks must specify language (```rust, ```bash, ```json)
- All JSON/YAML examples must be valid and parseable
- Use relative links for cross-references within the repo
- Tables for comparing options; bullets for lists of items
